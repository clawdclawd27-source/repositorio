{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "sh -c '[ -f package.json ] && npm ci || (cd projeto-aliens-ia/apps/api && npm ci)'"
  },
  "deploy": {
    "startCommand": "sh -c '[ -f src/server.js ] && npm start || (cd projeto-aliens-ia/apps/api && npm start)'",
    "healthcheckPath": "/health",
    "restartPolicyType": "on_failure"
  }
}
