# 2026-02-15

- Integração WhatsApp validada ponta a ponta no sistema da clínica: teste manual e disparos de consultas/aniversários executaram com sucesso, com logs `SENT` na tela de Notificações.
- Token da Meta foi rotacionado após erro de permissão `(#10) Application does not have permission for this action`; novo token aplicado no backend e envio voltou a funcionar.
- Tela de Notificações recebeu melhorias de UX operacional:
  - feedback explícito ao clicar em **Atualizar logs** (`Logs atualizados com sucesso`),
  - mensagens amigáveis para erros comuns da Meta (`#131030` allowlist e `#10` permissão de token),
  - orientação de formato do telefone e prévia normalizada.
- Foi criada uma página interna de operação diária: **Ajuda Operacional (POP)** em `/ajuda-operacional`, disponível para ADMIN/OWNER via atalho no Dashboard.
- Mitigação de instabilidade local (SIGKILL/code 137): scripts foram ajustados para separar `serve:stable` de `start:stable` em backend e frontend, permitindo subir sem rebuild contínuo e reduzindo quedas durante uso.
- Preferência da usuária reforçada: fluxo "faz e me mostra", com validação por prints, checkpoints curtos e execução autônoma.
- Domínio `clinicaemanuelleferreira.com` foi comprado no Cloudflare e a configuração DNS inicial foi realizada para acesso externo.
- Tentativa de publicação externa via túnel (`localhost.run`) foi considerada instável para operação contínua; túneis e registros temporários foram removidos por solicitação da usuária.
- Direção consolidada: abandonar túnel e seguir para deploy definitivo em Railway + domínio próprio.
- Railway foi configurado com projeto e banco Postgres online; bloqueio atual está no deploy do backend por ausência de repositório visível no GitHub dentro da integração da Railway.
- Estado atual da integração GitHub App (Railway): permissões confirmadas em `All repositories`, porém sem repositórios listados para seleção no Railway.
- Próximo passo objetivo acordado: criar um repositório GitHub (ex.: `clinica-app`) para habilitar a seleção na Railway e concluir o deploy sem túnel.
- Atualização de progresso: após `Refresh repositories`, o repo `clawdclawd27-source/clinica-app` passou a aparecer e foi conectado na Railway dentro do projeto `beautiful-alignment`.
- Serviço `clinica-app` já existe no Railway e está vinculado ao repo, porém o deploy ainda não inicia porque a branch conectada (`main`) está sem commit inicial (`Commit not found` / `Connected branch does not exist`).
- Pendência imediata para destravar deploy: criar/commitar `README.md` (ou qualquer arquivo) na `main` do repo `clinica-app`; depois disso, clicar em `Deploy` na Railway.
